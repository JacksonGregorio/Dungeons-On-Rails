<!DOCTYPE html>
<html>
<head>
    <title>Dungeons On Rails</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
<h1>Edit Character</h1>

<div class="container">

<%= form_for :character, local: true do |form| %>
  <% if @character.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(@character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% @character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div  class="form-group">
        <%= form.label :name_character, class: 'form-label' %>
        <%= form.text_field :name_character, class: 'form-control' %>
    </div>
    <div class="form-group">
        <%= form.label :lifepoints, class: 'form-label' %>
        <%= form.number_field :lifepoints, class: 'form-control' %>
    </div>

    <div class="actions">
        <%= form.submit class: "btn btn-primary"%>
    </div>
<% end %>


<%= link_to 'Back', "/characters/index" %>

</div>

</body>
</html>
